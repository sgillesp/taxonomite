<h1>Create a new node:</h1>

<%= form_for @node, :url => { :action => :create } do |f| %>
    <%= render 'shared/error_messages' %>

    <p>
        <%= f.label :name %><br />
        <%= f.text_field :name %>
    </p>

    <%= f.submit "Create" %>
<% end %>
